import { niceDate } from 'bike-rate/helpers/nice-date';
import { module, test } from 'qunit';

module('Unit | Helper | nice date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = niceDate([42]);
  assert.ok(result);
});
