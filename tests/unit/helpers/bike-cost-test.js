import { bikeCost } from 'bike-rate/helpers/bike-cost';
import { module, test } from 'qunit';

module('Unit | Helper | bike cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bikeCost([42]);
  assert.ok(result);
});
