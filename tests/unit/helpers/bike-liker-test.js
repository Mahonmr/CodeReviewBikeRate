import { bikeLiker } from 'bike-rate/helpers/bike-liker';
import { module, test } from 'qunit';

module('Unit | Helper | bike liker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bikeLiker([42]);
  assert.ok(result);
});
